name: data
description: Project data module
version: 0.0.1

environment:
  sdk: '>=3.0.3 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  core:
    path: ../core
  domain:
    path: ../domain

  dio: ^4.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner: ^2.4.5

flutter: