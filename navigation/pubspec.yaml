name: navigation
description: A navigation package.
version: 0.0.1

environment:
  sdk: '>=3.0.3 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  core:
    path: ../core
  core_ui:
    path: ../core_ui
  domain:
    path: ../domain
  home:
    path: ../features/home

  auto_route: ^7.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: 2.0.1
  dart_code_metrics: ^5.7.5
  auto_route_generator: ^7.1.1
  build_runner: ^2.4.5
